# Quick Request Template

Use this exact request to trigger the fixed scaffold flow:

```text
Generate the Web3 DApp scaffold using the fixed SOP:
1) create web/hardhat/generated/docs
2) init web with create-next-app latest
3) init hardhat with hardhat --init
4) remove defaults and keep clean skeleton
5) ensure web/src has app/components/contracts/hooks/lib/pages/services/shared/utils
6) add README.md in each of those folders
No extra design, no extra brainstorming.
```
