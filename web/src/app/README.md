# app

This folder contains Next.js App Router entries and global app-level files.

- `layout.tsx`: root layout and metadata
- `page.tsx`: default route (`/`)
- `globals.css`: global styles
- Route groups and feature routes should be added here
