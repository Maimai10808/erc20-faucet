# components

Reusable UI components for the frontend.

- Keep components presentational when possible
- Group by feature only if component scope is feature-specific
- Prefer small, composable pieces
